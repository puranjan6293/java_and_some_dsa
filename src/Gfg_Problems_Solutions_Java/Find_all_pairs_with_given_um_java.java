package Gfg_Problems_Solutions_Java;
import java.util.*;

public class Find_all_pairs_with_given_um_java {
    /*
    public pair[] allPairs( long A[], long B[], long N, long M, long X) {
        // Your code goes here
        ArrayList<pair> lis = new ArrayList<>();
        HashSet<Long> hs = new HashSet<>();
        for(long i :B){
            hs.add(i);
        }
        Arrays.sort(A);
        for(long i:A){
            if(hs.contains(X-i)){
                lis.add(new pair(i,X-i));
            }
        }
        pair[] list = new pair[lis.size()];
        return lis.toArray(list);
    }
     */
}
