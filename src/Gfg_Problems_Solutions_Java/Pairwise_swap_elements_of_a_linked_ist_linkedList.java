package Gfg_Problems_Solutions_Java;

public class Pairwise_swap_elements_of_a_linked_ist_linkedList {
    /*
    public Node pairwiseSwap(Node head)
    {
        // code here
        if(head==null || head.next==null){
            return head;
        }
        Node temp = head.next.next;
        Node first = head.next;
        Node second = head;

        head=first;
        head.next=second;
        second.next = pairwiseSwap(temp);

        return first;
    }
     */
}
