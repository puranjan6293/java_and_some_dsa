package Gfg_Problems_Solutions_Java;

public class Reverse_a_LinkedLst_in_groups_of_given_size {
    /*
    public static Node reverse(Node node, int k)
    {
        //Your code here
        //step1
        Node curr = node;
        Node prev = null;
        Node next = null;

        //step2
        int count =0;
        while(count<k && curr!=null){
            next = curr.next;
            curr.next = prev;
            prev = curr;
            curr = next;

            count++;
        }
        //step3, calling recursion
        if(next!=null){
            node.next = reverse(next,k);
        }
        return prev;
    }

     */
}
